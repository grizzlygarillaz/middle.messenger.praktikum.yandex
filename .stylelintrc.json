{
  "extends": "stylelint-config-recommended",
  "rules": {
    "color-hex-case": "lower"
  },
  "ignoreFiles": [
    "build/*",
    "dist/*"
  ],
  "overrides": [
    {
      "files": ["**/*.sass"],
      "customSyntax": "postcss-sass"
    }
  ]
}
