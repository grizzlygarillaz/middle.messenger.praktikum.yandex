<input type="{{ type }}"
       name="{{ name }}"
       value="{{ oldValue }}"
       title="{{ title }}"
       placeholder="{{ placeholder }}"
       {{#if required}}
          required
       {{/if}}
/>
