<input type="{{ type }}"
       name="{{ name }}"
       value="{{ value }}"
       title="{{ title }}"
       placeholder="{{ placeholder }}"
       {{#if required}}
          required
       {{/if}}
/>
